#include<bits/stdc++.h>

using namespace std;


int main() {
    cout<<"Before Function Call"<<endl;
    printf("hello");
    int a = 0;

    a = max(10,2);
    a = min(2, 30);
    a = pow(2, 10);
    a = sqrt(300);
    cout<<"After Function Call"<<endl;
    return 0;
}